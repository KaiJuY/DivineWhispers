# DivineWhispers Production Environment Variables
# Copy this to Render's Environment Variables section (NOT as a file)

# === CRITICAL SECURITY ===
SECRET_KEY=REPLACE_WITH_GENERATED_KEY
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# === DATABASE ===
# Render auto-provides this - DO NOT SET MANUALLY
# DATABASE_URL=postgresql+asyncpg://...

# === EMAIL (Zoho) ===
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_USER=REPLACE_WITH_YOUR_EMAIL
SMTP_PASSWORD=REPLACE_WITH_ZOHO_APP_PASSWORD
FROM_EMAIL=REPLACE_WITH_YOUR_EMAIL
FROM_NAME=Divine Whispers
SUPPORT_EMAIL=REPLACE_WITH_YOUR_EMAIL
FRONTEND_URL=REPLACE_AFTER_FRONTEND_DEPLOYS

# === OPENAI ===
OPENAI_API_KEY=REPLACE_WITH_OPENAI_KEY
LLM_MODEL=gpt-3.5-turbo
LLM_PROVIDER=openai

# === PAYMENTS (Optional - can add later) ===
# Stripe
STRIPE_SECRET_KEY=REPLACE_OR_LEAVE_EMPTY
STRIPE_PUBLISHABLE_KEY=REPLACE_OR_LEAVE_EMPTY
STRIPE_WEBHOOK_SECRET=REPLACE_OR_LEAVE_EMPTY

# TapPay
TAPPAY_PARTNER_KEY=REPLACE_OR_LEAVE_EMPTY
TAPPAY_MERCHANT_ID=REPLACE_OR_LEAVE_EMPTY
TAPPAY_APP_ID=REPLACE_OR_LEAVE_EMPTY
TAPPAY_APP_KEY=REPLACE_OR_LEAVE_EMPTY

# === CHROMADB ===
CHROMA_DB_PATH=/opt/render/project/chroma_db
CHROMA_COLLECTION_NAME=fortune_knowledge

# === CORS ===
ALLOWED_HOSTS=http://localhost:3000
# After frontend deploys, update to: https://your-frontend.onrender.com

# === APP SETTINGS ===
DEBUG=false
LOG_LEVEL=INFO
DEFAULT_USER_POINTS=100
FORTUNE_DRAW_COST=10
FORTUNE_INTERPRET_COST=15
